Я реализовал 2 способа загрузки даыннх по API:
 1-й Загружает "в лоб" по сути этим методом я смотрел на данные, их структуру,
 проверял какие поля возвращает API и всегда ли они одинаковые
 2-й более красив тем, что записывает данные в базу посредством только одного запроса
 и к тому же более правильный т.к. решает проблему присутствия кавычек в тексте

Локальный Postgres развернул, скриншот в репозитории

Что касается постановки на кроны, не слишком понял как это можно
 продемонстрировать ... В работе я использую Airflow и могу внести в код 
дженерик переменные, которые просто показывают, что скрипт стоит на расписании,
но так как Airflow у меня локально не развёрнут, мне легче будет при беседе рассказать
все, что про него знаю, а не отправлять код, который не работает.
А планировщик windows мне не понравился(
Если нужно будет, я могу поднять виртуалку линукса для кронов(хотя костыль).